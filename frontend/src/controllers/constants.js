export const ACCESS_TOKEN = "ACCESS_TOKEN";
export const ACCESS_TOKEN_HEADER = "access-token";
export const SERVER_API_URL = "http://localhost:5000";
//var full = window.location.protocol+'//'+ window.location.hostname + (window.location.port.toString().trim().length !== 0 ? ':'+ window.location.port: '');
